# Triangle Area Program

# Assertion 1: If k = 10 and j = 6, area should be 30
# Assertion 2: If k = 4 and j = 8, area should be 16

k = float(input("Enter base (k): "))
j = float(input("Enter height (j): "))

area = (k * j) / 2

print("Area =", area)